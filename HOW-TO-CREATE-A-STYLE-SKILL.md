# 如何复用「百图斩」创造一个属于你的风格 Skill

「百图斩」本身是可复制的模板。**以后只要改风格名 + 附 1–3 张参考图，把下面这段话发给任何 AI（Claude / Codex / Kimi / Hermes……），即可走完 分析风格 → 建档 → 出图 全流程。**

## 复用话术模板（直接复制发送）

> 我上传了 1–3 张参考图（见附件）。请你按「百图斩」技能（baituzhan）的流程为我处理：
>
> 1. **分析风格**：拆解这几张图的视觉特征，按八个维度填表——媒介 / 线条 / 上色方式 / 色板 / 构图 / 质感 / 题材倾向 / 画幅；多图找共性，差异标注"可变"。
> 2. **提炼锚定段与负面约束**：写一段英文锚定段（80–150 词，含 `{ratio}` 占位符，**不得出现真实画师姓名**，必须内置"生成图中不出现签名、日期、时间、水印"的禁令）和对应中文锚定段；在通用负面约束基础上按风格调整，但签名/日期/时间/水印四条必须保留。
> 3. **生成完整文件包**：把以上落成风格档案 `styles/<风格英文名>.md`（含风格拆解表、中英锚定段、负面约束清单、抽象概念→场景转译建议、默认画幅，格式含 `anchor-en` / `anchor-cn` / `negative` 三个代码块），参考图存入 `assets/`。
> 4. **两种使用模式**：之后我粘贴任意文章（模式A：提取 2–4 个关键场景逐张出图，风格统一）或给一句话需求（模式B：单图），用 `scripts/build_prompt.py --style <风格名>` 组装提示词出图；没有出图后端时输出完整中英双语提示词清单。
> 5. **实测跑案例**：先用新风格跑 1–2 张测试图目检，风格跑偏就回修锚定段与负面约束，直到稳定。

## 最小示例

```
[附上 2 张像素风参考图]
按百图斩流程斩这个风格，风格名 pixel-noir，建档案后用它给这句话出一张图：
"雨夜霓虹下的便利店门口，一个撑伞的人在等谁"
```

## 注意事项

- 参考图 1 张即可建档，2–3 张更稳（共性更明确）。
- 风格英文名用小写连字符（如 `pixel-noir`），档案与参考图命名保持对应。
- 话术第 2 步的两条禁令（无真实画师姓名、无签名日期水印）不可省略——前者防出图工具版权审核（HTTP 403），后者防模型模仿参考图上的签名日期。
