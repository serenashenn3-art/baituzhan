---
name: baituzhan
description: 百图斩 · 风格无关的通用复刻配图技能。两个阶段：1）斩风格（建档）——上传 1–3 张任意风格参考图，按来图识别 SOP（明度基调判定 → 逐区域放大目检带证据 → 骨架/可变特征分离）拆解并落成 styles/<风格名>.md 风格档案（含中英锚定段五个必备句、按参考图反推的负面约束、场景转译建议、默认画幅），再经样张核验闭环（出 1 张对图核验全绿）才算建成；2）出图——模式A：粘贴/上传任意文章，提取 2–4 个关键场景（场景须与明度基调兼容），逐张生成风格统一插图；模式B：一句话需求直接出单图。用 scripts/build_prompt.py 组装中英双语提示词（锚定段逐字复用 + 角色锚定 + 场景段 + 负面约束），调用可用出图后端（用户指定优先：GPT image2、nano banana；其次 Kimi image_generation 插件 --reference-image 锁风格；再次即梦/Midjourney），无后端时输出完整双语提示词清单优雅降级。适用触发：提到"百图斩""baituzhan""风格复刻配图""斩风格""按这张图的风格配图""统一风格插图"，或上传参考图要求复刻风格出图。
---

# 百图斩 · baituzhan

**风格无关**的通用复刻配图技能：给它 1–3 张参考图，斩出风格档案；再给它文章或一句话，批量产出风格统一的插图。`styles/` 里的档案（`watercolor-sketch`、`editorial-ink-wash`、`ligne-claire`、`naive-crayon`）都只是**已斩好的案例缓存**，可复用；核心能力是面对任意新图、任意新文章随时重走全流程。

## 两个阶段

### 阶段一：斩风格（建档）

用户上传 1–3 张任意风格参考图 →

1. **来图识别四步**（详见 `references/style-guide.md` 第一节，缺一不可）：
   - **溯源识人**：判断作者/流派并标置信度，查其整体作品提炼**风格谱系通用特征**；把"内容特征"（只对这张图成立）与"通用特征"（可复用进档案）分开；作者名只留档、绝不进提示词；多张参考图时**跨图互证**，只有各图都成立的不变量才算风格骨架；
   - 判**明度基调**（高调/中间调/低调，风格的第一指纹），并用脚本实测**量化指纹**（明度分布、饱和度、主色板及占比），结论可复核、不靠肉眼估；
   - **逐区域放大目检**（主体/线条最密处/色块边缘/背景留白/角落，共 3–5 个区域），按维度填拆解表，**每条结论必须带图中证据**，禁止凭风格名词套模板；
   - 区分**骨架特征**（明度、线条密度、色块层数、色板、留白、夸张度 → 进锚定段）与**可变特征**（题材、人物、构图 → 进场景段）。
2. 落成「风格档案」`styles/<风格英文名>.md`，内含：
   - 风格拆解表（带证据）
   - **英文锚定段**（逐字复用，含 `{ratio}` 占位符；**五个必备句**齐全：明度基调句 / 线条定量句 / 色块层数句 / 负向形态句 / 签名禁令句）
   - **中文锚定段**
   - **负面约束清单**（通用清单 + 按参考图反推"图里看不到的东西"；签名/日期/时间/水印四条永远保留）
   - 抽象概念→场景转译建议
   - 默认画幅
3. 把参考图复制到 `assets/`，命名与档案对应。
4. **锚定段严禁出现真实画师姓名**（会触发出图工具版权审核）；用通用描述 + 参考图锁风格。
5. **样张核验闭环**：先出 1 张样张 → 按 `references/style-audit-checklist.md` 与参考图逐项对照 → 全绿才批量；跑偏回修锚定段重出样张。批量时遵守**场景-明度兼容规则**（高调档案不选夜景孤灯场景）。

### 阶段二：出图

**模式 A：文章配图（输入为任意文章）**

1. **文章分析四步**（这是与"斩风格"并列的核心环节，不可跳过）：
   - **提炼核心**：一句话概括文章主题与情绪基调（如"困顿中的温柔坚持"），整套图都服务这句话；
   - **选关键场景**：2–4 个，优先级：冲突/转折时刻 > 核心人物动作 > 关键关系或场景氛围 > 抽象概念的场景化比喻。每场景一句中文：谁、在哪、做什么、什么情绪，并摘 1–2 句原文作依据；
   - **定角色锚定**：有人物主角的文章必须写一句话锁定外貌穿着（`--character`），全系列逐字复用；
   - **兼容转译**：按档案校验每个场景——场景-明度兼容（高调档案不选夜景孤灯）+ **场景-形态兼容**（单主体纯色背景类档案不画环境叙事，必须把场景提炼成一个象征物或小组合，按档案转译表执行）。
2. 为每个场景组装提示词（风格锚定段逐字复用 + 角色锚定 + 场景段 + 负面约束）：
   ```bash
   python scripts/build_prompt.py --style <风格名> --scene "<场景中文描述>"
   ```
   用户未指定风格时省略 `--style`，使用内置默认锚定；`--list-styles` 查看全部档案。
3. 出图（见下方「出图与降级」），同一篇文章所有插图使用**同一档案、同一画幅**，逐张生成。
4. 交付：图片文件（或提示词清单）+ 每张图对应的文章段落说明，建议插入位置。

**模式 B：简短需求（输入为一句话）**

```bash
python scripts/build_prompt.py --style <风格名> --scene "深夜办公室里，两名交易员隔着堆满报表的桌子争论"
```

## 出图与降级

1. 检查当前环境可用的图像生成能力，优先级：
   - **用户指定的工具**（GPT image2 / gpt-image 接口、nano banana / Gemini 图像生成等）——用户点名就先用；
   - Kimi 的 **image_generation 插件**：支持 `--reference-image` 传参考图锁风格，先把 `assets/` 里的对应参考图转成公共 URL，每张生成都带上；
   - 即梦、Midjourney 等其他本地可用工具。
2. 可用 → 逐张调用生成，画幅按档案默认（一般 16:9）或用户要求；英文提示词作主 prompt，负面约束一并附上（工具支持时）。
3. 不可用 → 明确告知"当前环境没有可调用的图像生成工具"，输出**完整详细的中英双语提示词清单**（锚定段 + 场景段 + 负面约束，逐条齐全），方便用户自行粘贴到任何出图工具。**不假装已出图。**
4. 生成后目检：是否符合档案拆解表特征、有无签名/日期/水印、有无照片写实感。跑偏时收紧负面约束重生成。

## 实测经验

- **prompt 里避免真实画师姓名**，会触发版权/内容审核（HTTP 403）。
- **生成图中不得出现签名、日期、时间、水印**：锚定段与负面约束均已内置禁令；参考图若带签名日期，生成后必须目检，被模仿出来就收紧负面约束重生成。
- **出图平台可能强制加盖"AI生成"角标**（服务端加盖，工具无开关，提示词无法去除）：提前告知用户，不要承诺去水印，也不做事后涂抹。
- **风格一致性靠参考图 + 锚定段双保险**：参考图走 `--reference-image`（或各工具的图生图/参考图参数），锚定段逐字复用。
- 带参考图若返回 HTTP 424（服务暂时不可用），去掉参考图重试。
- 2K + 16:9（2048×1152）适合公众号头图，已验证可用。

## 风格统一的三条铁律

1. 锚定段逐字复用，每次生成都带完整锚定段，不缩写、不凭记忆改写。
2. 同一批插图共用同一风格档案与画幅；场景描述只换人物/动作/环境。
3. 负面约束每次必带，其中 no artist signature / no dates / no timestamps / no watermark 四条永远保留。
